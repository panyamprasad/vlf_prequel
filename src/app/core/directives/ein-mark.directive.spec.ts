import { EinMarkDirective } from './ein-mark.directive';

describe('EinMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new EinMarkDirective();
    expect(directive).toBeTruthy();
  });
});
